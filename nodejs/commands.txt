git --version
docker --version
minikube version
minikube status
systemctl status Jenkins
jenkins --version
ps -ef | grep Jenkins

kubectl version --client
kubectl config current-context

kubectl config use-context minikube

dockerfile

docker login

docker build -t node-js-sample .
docker run -p 5000:5000 node-js-sample

docker push karthick1409/node-js-sample:latest
docker tag node-js-sample karthick1409/node-js-sample:1.0 


docker run -p 30007:5000 karthick1409/node-js-sample:1.0

kubectl apply -f k8s/persistent.volume.yaml
kubectl apply -f k8s/pvc.yaml
kubectl apply -f k8s/deplyment.yaml


kubectl apply -f node-js-sample/policy.yaml
kubectl get networkpolicy
kubectl label pod <allowed-pod-name> access=allowed

kubectl get pods nginx-pod --show-labels


kubectl exec -it <allowed-pod-name> -- curl http://nodejs-service:5000
kubectl exec -it <unlabeled-pod-name> -- curl http://nodejs-service:5000

kubectl label pod  pod/nodejs-app-565f6d75d5-v2bfk access=allowed


